package com.freelance.training.vehicle;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * @author rahul
 * This is the main class of application from which the
 * spring boot application will get executed
 */
@SpringBootApplication
public class VehicleBackendApplication {

	public static void main(String[] args) {
		SpringApplication.run(VehicleBackendApplication.class, args);
	}

}
